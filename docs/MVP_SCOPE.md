# MVP scope

Gateway keeps OpenAI-compatible HTTP, SQLite session state, deterministic routing,
bounded contexts, review gating, loop blocking, and systemd profile ownership.

`scripts/run-mvp-benchmark.sh` uses ten local Git fixtures and a deterministic
OpenCode-compatible synthetic loop. It is not evidence of a physical remote
OpenCode client or heavy-judge runtime.
